{
  "rules": {
    "presence": {
      "$userId": {
        ".read": "true",
        

        ".write": "auth != null && auth.uid === $userId",
        

        "status": { ".validate": "newData.isString()" }
      }
    }
  }
}
