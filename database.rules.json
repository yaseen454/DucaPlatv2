{
  "rules": {
    "presence": {
      "$userId": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $userId",
        "$sessionId": {
           ".validate": "newData.hasChildren(['status', 'lastActive'])"
        }
      }
    }
  }
}
